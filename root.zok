def main(private u32 a, private u32 b, private u32 c, u32 x) -> bool {
    // quadratic equation: a*x^2 + b*x + c = 0
    u32 lhs = a * x * x + b * x + c;

    u32 rhs = 0;

    assert(lhs == rhs);

    // return true if no assertion errors occur
    return true;
}
